fastapi
uvicorn[standard]==0.22.0
openai
pydantic-settings

# --- LlamaIndex & Unstructured stack (compatible with Python 3.12+)
unstructured[pdf,docx,local-inference]
llama-index==0.10.50
llama-index-readers-file

# --- Other dependencies
git+https://github.com/openai/whisper.git
pydub
docx2txt
nbconvert
openpyxl
chromadb
scipy>=1.11.2,<1.12
sentence-transformers
pypdf

# --- System Dependencies (Install via apt/brew, not pip) ---
# tesseract-ocr (Required for OCR in Advanced Indexing) -> sudo apt install tesseract-ocr
# poppler-utils (Required for PDF processing in Advanced Indexing) -> sudo apt install poppler-utils
